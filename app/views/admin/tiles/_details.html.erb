<p>
  <strong>What3Words:</strong>
  <%= render_w3w_code(tile) %>
  - <%= link_to 'view on W3W', tile.w3w_url %>
</p>

<p>
  <strong>South West:</strong>
  <%= rounded_coordinates(@tile.southwest) %>
</p>

<p>
  <strong>North East:</strong>
  <%= rounded_coordinates(@tile.northeast) %>
</p>

<p>
  <strong>Midpoint:</strong>
  <%= rounded_coordinates(@tile.midpoint) %>
</p>

<p>
  <strong>Plot:</strong>
  <% if @tile.plot.present? %>
    <%= link_to @tile.plot.title, admin_plot_path(@tile.plot) %>
  <% else %>
    -
  <% end %>
</p>

<p>
  <strong>Project:</strong>
  <% if @tile.project.present? %>
    <%= link_to @tile.project.title, admin_project_path(@tile.project) %>
  <% else %>
    -
  <% end %>
</p>
