<html>
<% _available, _unavailable, combined_tiles = @tile.plot&.tiles_for_map(include_tile: @tile) %>
<%= render 'static_pages/w3w_map',
    tiles: combined_tiles || [@tile],
    plot: @tile.plot,
    hide_legend: true,
    tile_legend: [
      { key: 'highlighted', title: 'N/A', tiles: [@tile], colour: 'gold', action: :view_details_or_unavailable }
    ],
    map_height: '100%' %>
</html>
