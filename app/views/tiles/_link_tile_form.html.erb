<%= form_with url: link_tile_subscription_path(subscription) do |form| %>
  <%= form.hidden_field :tile, value: tile.hashid %>
  <%= form.submit 'Claim this tile', class: 'btn btn-primary' %>
  using your existing subscription.
<% end %>
